---
description: Paste image from Windows clipboard (WSL workaround)
---

Run `bash ~/.config/opencode/scripts/paste-img.sh`

If an image was found, analyze @/tmp/opencode-paste.png and $ARGUMENTS
If no image was in the clipboard, tell me to copy an image first.
